@model IEnumerable<Api_task.Models.Facility>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Faculity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Facilities1.Faculity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Facility1.Faculity)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Faculity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Facilities1.Faculity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Facility1.Faculity)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
